<!DOCTYPE html> <html lang="ja"> <head> <meta charset="UTF-8" /> <meta name="viewport" content="width=device-width, initial-scale=1.0" /> <title>パスワードを忘れた方</title> <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen flex items-center justify-center bg-blue-50 text-gray-800">

  <div class="bg-white shadow-lg rounded-lg p-8 w-full max-w-md">
    <h1 class="text-2xl font-bold text-blue-600 mb-6 text-center">パスワードを忘れた方</h1>

    <form action="/forgot-password" method="POST" class="space-y-4">
      <div>
        <label for="userId" class="block text-sm font-medium">ユーザーID</label>
        <input
          type="text"
          id="userId"
          name="userId"
          required
          class="w-full mt-1 px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
      </div>

      <button
        type="submit"
        class="w-full bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 transition font-semibold"
      >
        確認
      </button>
    </form>

    <% if (message) { %>
      <p class="text-green-600 text-sm mt-4"><%= message %></p>
    <% } %>

    <div class="mt-6 text-center">
      <a href="/" class="text-sm text-blue-600 hover:underline">ログインページに戻る</a>
    </div>
  </div>

</body>
</html>