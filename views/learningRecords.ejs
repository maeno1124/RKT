<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>学習記録</title>
</head>
<body>
  <h1>学習記録</h1>
  <table border="1">
    <thead>
      <tr>
        <th>ユーザーID</th>
        <th>学習時間（秒）</th>
        <th>日付</th>
        <th>月</th>
        <th>年</th>
        <th>記録日時</th>
      </tr>
    </thead>
    <tbody>
      <% if (records && records.length > 0) { %>
        <% records.forEach(record => { %>
          <tr>
            <td><%= record.user_id %></td>
            <td><%= record.duration %> 秒</td>
            <td><%= record.date %></td>
            <td><%= record.month %></td>
            <td><%= record.year %></td>
            <td><%= record.created_at %></td>
          </tr>
        <% }); %>
      <% } else { %>
        <tr>
          <td colspan="6">学習記録が存在しません。</td>
        </tr>
      <% } %>
    </tbody>
  </table>
</body>
</html>
